> [6. Documentación de Arquitectura (Bosquejo)](../6.md) › [6.4. Otras Vistas](6.4.md)

# 6.4. Otras Vistas

CODIGO STRUCTURIZ:

workspace "Lyra - Arquitectura General + Pagos" "C4 completo" {
  model {
    # ===============================
    #        PERSONAS
    # ===============================
    user_final = person "Usuario Final" "Usa la app Lyra para generar playlists por emoción."
    admin_soporte = person "Administrador / Soporte" "Administra la plataforma."
    # ===============================
    #     SOFTWARE SYSTEM PRINCIPAL
    # ===============================
    lyra = softwareSystem "Lyra Platform" "Plataforma que detecta emoción, genera playlists y gestiona pagos." {
      # ===============================
      #            CONTAINERS
      # ===============================
      frontend = container "Frontend App" "Flutter" "UI móvil del usuario. Firma peticiones (HMAC)."
      api_gateway = container "API Gateway" "Node.js" "Routing, JWT, Zero Trust."
      # --- Microservicio de Preferencias
      ms_pref = container "Microservicio de Preferencias del Usuario" "NestJS" "Preferencias, canciones favoritas."
      db_pref = container "Base de Datos de Preferencias y Canciones" "MongoDB" "Almacena preferencias, ratings."
      # --- Microservicio de Planes y Suscripciones (REEMPLAZA AL DE PAGOS)
      ms_planes = container "Microservicio de Planes y Suscripciones" "NestJS" "Procesa pagos, planes, intents y suscripciones." {
        # Componentes existentes
        stripe_controller = component "StripeController"  "Controller que crea PaymentIntents y recibe webhooks."
        payments_controller = component "PaymentsController" "Expone historial de pagos y suscripciones."
        stripe_service = component "StripeService" "Integración con Stripe."
        payments_service = component "PaymentsService" "Registra y consulta pagos y renovaciones."
        payment_repo = component "PaymentRepository" "TypeORM Repository para MySQL."
        
        # Nuevos Componentes (Implementados en esta sesión)
        circuit_breaker = component "CircuitBreakerService" "Patrón de disponibilidad. Monitorea salud de Stripe."
        backup_service = component "BackupPaymentService" "Lógica de Fallback. Integra Braintree."
        audit_service = component "AuditService" "Registra operaciones críticas (Zero Trust)."
        signature_guard = component "SignatureGuard" "Verifica integridad de mensajes (HMAC-SHA256)."
      }
      db_planes = container "Base de Datos de Planes y Pagos" "MySQL" "Planes, precios, suscripciones e historial."
      # --- Microservicio de Generación de Playlists
      ms_playlist = container "Microservicio de Generación de Playlists" "NestJS" "Genera playlists personalizadas."
      # --- Microservicio de Gestión de Usuarios
      ms_usuarios = container "Microservicio de Gestión de Usuarios" "NestJS" "Login, datos del usuario."
      db_usuarios = container "Base de Datos de Usuarios" "PostgreSQL" "Almacena datos del usuario."
      # --- Microservicio de Conexión Externa
      ms_external = container "Microservicio de Conexión con Plataformas Externas" "NestJS" "Envía playlists a Spotify / Apple."
    }
    # ===============================
    #        SISTEMAS EXTERNOS
    # ===============================
    idp_auth = softwareSystem "IdP / Autenticación" "Login, sesiones, 2FA."
    spotify_api = softwareSystem "Spotify API" "Proveedor de música externo."
    apple_api = softwareSystem "Apple Music API" "Proveedor externo de música."
    stripe = softwareSystem "Stripe" "Procesamiento de pagos principal."
    braintree = softwareSystem "Braintree (PayPal)" "Procesamiento de pagos de respaldo (Fallback)."
    # ===============================
    #          RELACIONES
    # ===============================
    # Personas
    user_final -> frontend "Usa la app"
    admin_soporte -> lyra "Opera y monitorea"
    # Flujo general
    frontend -> api_gateway "Solicita datos y funciones (Firmadas)"
    api_gateway -> ms_pref "Preferencias de usuario"
    api_gateway -> ms_planes "Consulta planes, pagos y suscripciones"
    api_gateway -> ms_playlist "Generación de playlists"
    api_gateway -> ms_usuarios "Autenticación y datos de usuario"
    api_gateway -> ms_external "Exportación de playlists"
    # Relaciones Internas de ms_planes (Componentes)
    api_gateway -> stripe_controller "Envía petición de pago"
    api_gateway -> payments_controller "Consulta historial y suscripciones"
    stripe_controller -> signature_guard "Verifica integridad"
    stripe_controller -> stripe_service "Intenta procesar con Stripe"
    stripe_service -> circuit_breaker "Protege llamada"
    stripe_service -> backup_service "Activa Fallback (si CB abre)"
    circuit_breaker -> stripe "Llama a API (si circuito cerrado)"
    
    # Flujo de Fallback
    stripe_controller -> backup_service "Usa si Circuit Breaker abre"
    backup_service -> braintree "Procesa pago de respaldo"
    
    # Persistencia y Auditoría
    stripe_controller -> payments_service "Registra transacción"
    stripe_controller -> audit_service "Audita operación"
    payments_service -> payment_repo "Guarda datos"
    payment_repo -> db_planes "SQL Read/Write"
    # Bases de datos
    ms_playlist -> db_pref "Lee preferencias para generacion de playlists"
    ms_pref -> db_pref "Lee/Escribe preferencias"
    ms_usuarios -> db_usuarios "Lee/Escribe datos del usuario"
    # Servicios Externos
    ms_external -> spotify_api "Envía playlists"
    ms_external -> apple_api "Envía playlists"
    lyra -> idp_auth "Autenticación"
    stripe -> ms_planes "Webhook de confirmación"
  }
  # ===============================
  #                VISTAS
  # ===============================
  views {
    systemContext lyra {
      include *
      autoLayout
    }
    container lyra {
      include *
      autoLayout
    }
    component ms_planes {
      include *
      autoLayout
    }
    styles {
      element "Person" {
        background "#08427b"
        color "#ffffff"
      }
      element "SoftwareSystem" {
        background "#1168bd"
        color "#ffffff"
      }
      element "ExternalSystem" {
        background "#dddddd"
        color "#000000"
      }
      element "Container" {
        background "#438dd5"
        color "#ffffff"
      }
      element "Component" {
        background "#85bbff"
        color "#000000"
      }
    }
  }
}

---

[⬅️ Anterior](../6.3/6.3.md) | [🏠 Home](../../README.md)